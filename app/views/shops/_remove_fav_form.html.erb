<%= form_with(model: current_user.shop_favorites.find_by(shop_id: @shop.id ), html:{ method: :delete }, local: false) do |form|  %>
    <!-- @todo if user added the product to favor list i class should be bi bi-hearth-fill -->
    <% if user_signed_in? %>
      <%= form.submit 'Remove Favorite', id: "add-fav" %>
    <% end %> 
<% end %>   